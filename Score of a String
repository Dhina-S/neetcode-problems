class Solution {
    public int scoreOfString(String s) {
     char c[]=s.toCharArray();
     int sum=0,r=0;
     for(int i=1;i<c.length;i++){
        for(int j=i-1;j<i;j++){
        sum+=Math.abs((c[i]-c[j]));
        }
     }
     return sum;
    }
}
